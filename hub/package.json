{
  "name": "claude-mobile-hud-hub",
  "version": "0.0.1",
  "description": "Long-lived Node daemon. Phone と HTTP/SSE で通信し、Bridge との loopback IPC を中継する。Phase 3 §5 参照。",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p .",
    "start": "tsx --env-file-if-exists=.env src/index.ts",
    "pair": "tsx --env-file-if-exists=.env src/pair.ts",
    "pair:lan": "tsx --env-file-if-exists=.env src/pair.ts lan",
    "pair:ts": "tsx --env-file-if-exists=.env src/pair.ts ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "qrcode-terminal": "^0.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/qrcode-terminal": "^0.12.2",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
