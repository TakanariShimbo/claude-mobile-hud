// Placeholder smoke test so `npm test` passes from day 1.
// Phase 4 で実テストに置き換える。
import { describe, it, expect } from "vitest";

describe("hub", () => {
    it("scaffolding sanity check", () => {
        expect(true).toBe(true);
    });
});
