// Placeholder smoke test. Phase 4 で実テスト追加。
import { describe, it, expect } from "vitest";

describe("bridge", () => {
    it("scaffolding sanity check", () => {
        expect(true).toBe(true);
    });
});
