{
  "name": "claude-mobile-hud-bridge",
  "version": "0.0.1",
  "description": "MCP server per Claude Code session. Claude Code が --mcp-config 経由で起動。Phase 3 §6 参照。",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p .",
    "start": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
