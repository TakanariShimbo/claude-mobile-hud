# OS
.DS_Store
Thumbs.db

# Editors / IDE
.idea/
.vscode/
*.swp
*.swo

# Android / Gradle (preempt: phase 4 で発生する)
.gradle/
build/
local.properties
captures/
*.iml

# Node (hub/bridge 側で発生する想定)
node_modules/
dist/
*.log
.env
.env.local
# Phase 4: package-lock.json は CI (`actions/setup-node` の cache: npm) と
# Bridge / Hub の reproducible install のために commit する。

# Python (もし使うことになったら)
__pycache__/
*.pyc
.venv/

# Generated
*.tmp
# `claude-mobile-hud run` が毎回 uuid 入りで生成する MCP config。
# session_id だけなので機密ではないが、generated file を repo に紛れ込ませない (P1-2)。
bridge/.mcp.runtime.json
